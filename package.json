{
  "name": "tether-monorepo",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.7"
  },
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=18"
  }
}
